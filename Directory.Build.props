<Project>

   <Import Project="$(MSBuildThisFileDirectory)\NeosInstall.props" />

   <PropertyGroup>
      <NeosLibDir>$(NeosInstallDir)\Neos_Data\Managed</NeosLibDir>
      <NeosExternalLibDir>$(NeosInstallDir)\Libraries</NeosExternalLibDir>
   </PropertyGroup>

   <PropertyGroup Condition="$(ToNeos) != ''" >
     <PublishDir>$(NeosExternalLibDir)</PublishDir>
   </PropertyGroup>

</Project>
